export type AmountType = "price" | "percentage" | "number";

export type LiquidityType = {
  name: string;
  liquidity: string;
  volume24h: string;
  lpRewardApr: string;
  totalFees: string;
  address: string;
};
